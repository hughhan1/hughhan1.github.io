import * as React from 'react';

import Footer from '../footer';

interface Props {}

export const BlogList: React.SFC<Props> = (props: Props) => {
  return <div className="blog-list" />;
};

export default BlogList;
